function codespace() {
  brew install fzf nvim

  if [ -f ./script/build-ctags ] then
    echo "installing ctags"
    brew install --HEAD universal-ctags/universal-ctags/universal-ctags
    echo "building ctags"
    script/build-ctags > /dev/null 2>&1
  fi
}
