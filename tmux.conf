# improve colors
set -g default-terminal 'screen-256color'

# act like vim
setw -g mode-keys vi
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R
bind-key -r C-h select-window -t :-
bind-key -r C-l select-window -t :+

# start window numbers at 1 to match keyboard order
set -g base-index 1
set-window-option -g pane-base-index 1

# renumber windows sequentially after closing any of them
set -g renumber-windows on

# increase scrollback lines
set -g history-limit 10000

# don't suspend-client
unbind-key C-z

# --- local overrides (formerly tmux.conf.local) ---

# remap prefix to Control + a
set -g prefix C-a
bind C-a send-prefix
unbind C-b

# enable mouse support (click to focus panes, drag to resize, scroll)
set -g mouse on

# mouse drag selects within pane and copies to system clipboard on release
set -g copy-command "pbcopy"
bind -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel

# vim-style copy mode: v to select, y to yank to system clipboard
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "pbcopy"

# resize panes with prefix + Option+Arrow
bind -r M-Up resize-pane -U 5
bind -r M-Down resize-pane -D 5
bind -r M-Left resize-pane -L 5
bind -r M-Right resize-pane -R 5

# allow Shift+Enter to insert newline in Copilot CLI
bind -n S-Enter send-keys Escape Enter

# dim inactive panes to make focused pane obvious
# colors set by `theme` script; these are solarized dark defaults
set -g window-style 'bg=#001a26'
set -g window-active-style 'bg=#002b36'

# thick bright border on active pane
set -g pane-active-border-style 'fg=#859900,bold'
set -g pane-border-style 'fg=colour240'
set -g pane-border-lines heavy

# label each pane: shows pane title or current path
set -g allow-passthrough on
set -g pane-border-status top
set -g pane-border-format ' #{?#{!=:#{pane_title},},#{pane_title},#{pane_current_path}} '

# --- status bar ---
set -g status-position bottom
set -g status-style 'bg=#073642,fg=#839496'

# left: window list
set -g status-left ''
set -g status-left-length 0

# right: git branch + session name
set -g status-right '#[fg=#586e75]#{?#{==:#{pane_current_command},zsh},#(cd #{pane_current_path} && git branch --show-current 2>/dev/null),} #[fg=#268bd2,bold] #{session_name}'
set -g status-right-length 60

# monitor windows for activity/bells
setw -g monitor-activity on
setw -g monitor-bell on
set -g visual-activity off

# window tabs: number:name with active highlight + alert indicator
set -g window-status-format '#[fg=#586e75] #I:#W#{?window_bell_flag,🔔,#{?window_activity_flag,•,}} '
set -g window-status-current-format '#[fg=#eee8d5,bg=#268bd2,bold] #I:#W '
set -g window-status-separator ''
set -g window-status-bell-style 'fg=#dc322f,bold'
set -g window-status-activity-style 'fg=#b58900'

# session navigator: prefix+S
bind S display-popup -E -w 80% -h 80% "$HOME/.local/bin/tmux-nav sessions"

# window navigator: prefix+W
bind W display-popup -E -w 80% -h 80% "$HOME/.local/bin/tmux-nav windows"
